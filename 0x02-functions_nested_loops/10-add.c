#include "holberton.h"
/**
 * add - adds two integers and returns the result.
 * @a: Any number.
 * @b: Number to be added to a.
 * Return: result of added a + b.
 */
int add(int a, int b)
{
	return (a + b);
}
