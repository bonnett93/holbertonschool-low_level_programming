#include "holberton.h"

/**
 * mul - take two numbers and return the multiplication.
 * @a: The first numer for multiplication.
 * @b: The second number for multiplication.
 * Return: the result of multiplying a * b.
 */
int mul(int a, int b)
{
	return (a * b);
}
